import { OneTimeBindingPipe } from './one-time-binding.pipe';

describe('OneTimeBindingPipe', () => {
  it('create an instance', () => {
    const pipe = new OneTimeBindingPipe();
    expect(pipe).toBeTruthy();
  });
});
